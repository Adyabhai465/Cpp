#include <iostream>
using namespace std;
int main () {
    int n;
    cout<<"Enter the number : ";
    cin>>n;
    (n%2==0) ? cout<<"The Number is even." : cout<<"The Number is odd." ;  //Ternary Operator
}
